VS2015+CUDA9.1
main.cu:
You need to configure the corresponding environment, then
create a new project, copy the code here, and modify the 
corresponding path.

data:
You need to use MATLAB to convert the image file into a data file,
 and then use the CUDA code to process the data file. For the 
MATLAB code of the .ers file, refer to the read-ers file
